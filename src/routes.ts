const BACKEND_URL = process.env.REACT_APP_BASE_URL
export const API_URLS = {
    userURL: `${BACKEND_URL}/`,
    paymentURL:`${BACKEND_URL}/payments`,
    debtURL:`${BACKEND_URL}/debts`,
    earningURL:`${BACKEND_URL}/earnings`
}

